services:
  app:
    build: .
    container_name: super-admin
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3051"
    networks:
      - app-network
 
networks:
  app-network:
    external: true
    name: amp_portal_backend_app-network
 